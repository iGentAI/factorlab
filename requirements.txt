# Pinned to the versions the archived results were produced with (Python 3.12.3, Ubuntu 24.04).
# System packages: libfplll-dev fplll-tools libgmp-dev libmpfr-dev libqd-dev (for fpylll).
# cypari2 2.2.4 bundles PARI/GP 2.17.2 in its wheel; no separate PARI installation is needed.
gmpy2==2.3.1
python-flint==0.9.0
fpylll==0.6.4
cypari2==2.2.4
numpy==2.4.6
scipy==1.17.1
sympy==1.14.0
# mpmath is not imported directly; it is sympy's numerical backend and is pinned so the stack is fully reproducible.
mpmath==1.3.0
pytest==9.0.3
